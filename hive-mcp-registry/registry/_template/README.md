# Template README

Use this folder as a copy-ready starting point for a new registry submission. Replace the placeholder metadata in `manifest.json`, add your real installation and authentication details, and then copy both files into `registry/servers/<your-server>/`.
