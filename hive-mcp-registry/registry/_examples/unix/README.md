# PostgreSQL Unix Socket Example

This example models a PostgreSQL MCP server that communicates over a Unix domain socket. It demonstrates socket templates, database-oriented tools, and credentials typically required for secure database access.
